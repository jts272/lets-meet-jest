// Write minimum code required to pass the addition function:

// function addition() {
//   return 42;
// }

// Export the function so the test can read it:

// Write minimum code to pass first and second test:

// function addition(n1, n2) {
//   return n1 + n2;
// }

// Try arrow function:

const addition = (n1, n2) => n1 + n2;

// NOTE: Arrow functions must be declared before `module.exports`

module.exports = addition;
